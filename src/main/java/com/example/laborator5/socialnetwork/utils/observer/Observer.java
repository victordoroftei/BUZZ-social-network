package com.example.laborator5.socialnetwork.utils.observer;

/**
 * This is the interface for the Observer which is a component of the observer pattern
 */
public interface Observer {

    /**
     * This method is called when an observable notifies all the observers
     */
    void update();
}
